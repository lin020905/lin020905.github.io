Place a licensed loop file named `dreamworld-loop.mp3` in this directory.
The player also tries `dreamworld-loop.MP3` for case-sensitive hosts.

The site will try to play that file first. If the file is missing or fails to
load, the player falls back to the built-in generated loop.
